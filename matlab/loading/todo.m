function to_do()

%to_do.get_tcv_exp  = 'z_axis in Bnd';
to_do.rhosOK       = 'plot several astra_out but not all times';
to_do.plotAstra    = 'merge with check_compute_needed, many t_sim';
to_do
clear to_do;
end
